// src/game/test/helpers/makeState.js
// Placeholder for makeState helper if needed for future stateful tests.
export const makeState = (overrides = {}) => ({
  // Add default state shape here
  ...overrides,
});
